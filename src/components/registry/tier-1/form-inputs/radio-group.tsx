// AUTO-GENERATED BY npm run generate:registry-shells
import { mergeProps, splitProps } from "solid-js";
import type { JSX } from "solid-js";
import { RegistryComponentShell } from "~/components/registry-component-shell";
import { registryItemBySlug } from "~/lib/registry";

export type RadioGroupProps = JSX.HTMLAttributes<HTMLElement> & {
  class?: string;
};

export function RadioGroup(userProps: RadioGroupProps) {
  const props = mergeProps(userProps);
  const [local, others] = splitProps(props, ["class", "children"]);

  return (
    <RegistryComponentShell item={registryItemBySlug["radio-group"]} class={local.class} {...others}>
      {local.children}
    </RegistryComponentShell>
  );
}
